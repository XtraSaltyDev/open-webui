# Native macOS Feature Parity Matrix

This matrix is based on the current checkout's Svelte routes, backend routers, backend models, and configuration flags.

| Area | Open WebUI Evidence | Native Status | Target Phase |
| --- | --- | --- | --- |
| Chat shell | `src/lib/components/chat/Chat.svelte`, `src/routes/(app)/c/[id]` | MVP in progress with active-provider chat capability gating before sends | 1 |
| Chat history | `backend/open_webui/routers/chats.py`, `models/chats.py` | Chat persistence, rename/delete, clone, pin/unpin with pinned-first sorting, archive/unarchive with archived-chat recovery, archive-all/unarchive-all, confirmed delete-all, app-local chat/message links, all-chat and archived-chat JSON export, `tag:`/`folder:`/`pinned:`/`archived:` search operators, native message transcript search, single-chat JSON export/import, Open WebUI-compatible single-chat and all-chat export, all-chat export-bundle import, and tolerant Open WebUI chat record/envelope active-branch import implemented; fuller Open WebUI compatibility planned | 1/2 |
| Folders/tags | `routers/folders.py`, `models/folders.py`, `models/tags.py` | Folders and chat tags implemented, with folder create/assignment/delete actions blocked when the Folders feature is disabled | 2 |
| Markdown/code/LaTeX | `marked`, `katex`, `shiki`, chat components | Basic markdown, fenced code blocks with copy actions, lightweight native token coloring for common languages, and inline/block math display implemented; full math typesetting and Shiki-style syntax highlighting planned | 2 |
| Citations/sources | Chat events `source` and `citation` in `Chat.svelte` | Knowledge citations shown, exported, and linked to indexed source document previews with cited chunk focus | 2/4 |
| Attachments/files | `routers/files.py`, `models/files.py` | Text and selectable-PDF attachments implemented for chat context/export, reusable local file-library persistence with original imported bytes when available, native Files sidebar/detail surface with free-text search, Open WebUI-style wildcard filename search, broad Files-only binary import/export/share-original-file, attach/share-extracted-text/copy-extracted-text/export-extracted-text/edit-extracted-text/rename/individual-delete/delete-all actions, native JSON import/export preserving original bytes, tolerant Open WebUI-shaped file-record import, Open WebUI-shaped file-record export, attach-from-saved-file composer menu gated to records with extracted text, disabled-feature blocking for saved file-library import/use/mutation/JSON import/file and open-panel JSON import entry points, and workspace-backup inclusion; richer parsing, OCR, multimodal attachment context, and fuller Open WebUI Files parity planned | 3/4 |
| Multi-model chat | `selectedModels` in `Chat.svelte` | Multi-select fan-out, concurrent branch streaming, active-branch progress text, elapsed generation-time labels, per-branch error handling, whole-send cancellation, per-branch cancellation, and provider stream-task cancellation implemented | 2 |
| Message actions | regenerate/share/export/rate/edit in chat components/routes | Copy, edit, regenerate with active-provider chat capability preflight, rate, Markdown copy/share, JSON export implemented | 2 |
| Ollama | `routers/ollama.py`, config `OLLAMA_BASE_URLS` | Runtime status/health checks with version, installed model count, and running model count; model listing, cancellable streaming chat, raw prompt completions through `/api/generate`, pull, delete, and model-management capability gating implemented | 1 |
| OpenAI-compatible APIs | `routers/openai.py`, config `OPENAI_API_BASE_URLS` | Health checks, model list, cancellable streaming chat, raw prompt completions through `/completions`, and embeddings implemented | 3 |
| Keychain secrets | Web app uses server/local config | Implemented for provider API keys, including cleanup when removing an OpenAI-compatible provider | 3 |
| OpenAI account OAuth | Official API docs currently use bearer credentials from API keys or short-lived workload identity federation access tokens; ChatGPT/API billing is separate | Native account-access policy and Settings status implemented with official reference links and last-review date; ChatGPT subscription model access is blocked unless officially supported, with cookie reuse/private endpoints explicitly disallowed | 3 |
| Embeddings | `/api/embeddings`, RAG embedding config | OpenAI-compatible request path, dedicated embedding model setting, provider-aware embedding model candidates, and conservative fallback for unlabeled custom gateways implemented | 3/4 |
| Knowledge/RAG | `routers/knowledge.py`, `routers/retrieval.py`, `retrieval/*` | Text/selectable-PDF/native-note collections, collection rename, user/group collection access grants, grant-aware sidebar visibility/document preview/citation opening/`#` retrieval, document browsing/detail/rename/deletion, chunking, embeddings, retrieval, `#` context, file-based reindex, embedding-model selection with likely embedding-model defaulting, active-provider embedding capability gating, disabled-feature blocking before collection/document mutations, provider embedding calls, JSON import, document preview selection, `#collection` retrieval/provider chat, and single-collection sharing, `knowledge.write` admin-directory enforcement for write operations, permission-aware sidebar management UI, native JSON backup/restore, and single-collection native JSON sharing implemented; richer workflows planned | 5 |
| Vector storage | `retrieval/vector/*`, Chroma and other deps | JSON vector snapshot for MVP; production local-first backend planned | 4 |
| Prompts | `routers/prompts.py`, `models/prompts.py` | Native prompt library implemented with create/edit/delete, command editing, tag/category editing, user/group access-grant editing, sidebar metadata display, slash-command composer expansion, JSON persistence, local audit events for create/update/delete, variable-aware composer insertion for `{{variable}}` placeholders, disabled-feature blocking for prompt writes/imports/file and open-panel import entry points/insertions/slash-command lookup/sharing, `prompts.write` admin-directory enforcement for write operations, native JSON export/import with count-only audit metadata, raw Open WebUI prompt-record export with `access_grants`, single-prompt native sharing, tolerant Open WebUI-style JSON import preserving tags and access grants, and previous-version snapshot migration on prompt update; broader admin-level prompt management remains planned | 5 |
| Tools | `routers/tools.py`, `tools/*` | Native local tool library implemented with create/edit/delete, JSON-object valves editing/validation, schema-derived valves JSON defaults, generated primitive valves field controls, save-time valves schema validation including required/type/nullable-union/const/anyOf/oneOf/allOf/nested/enumerated/string/numeric/pattern/multiple/unique/additional-property constraints, `tools.write` admin-directory enforcement for write operations, JSON persistence, local audit events for create/update/delete, native/Open WebUI JSON export, single-tool native sharing, tolerant Open WebUI-style tool record import preserving specs and valves, disabled-feature blocking for tool create/update/delete/import/share plus local Python `Tools` method test-runs and `Valves` schema discovery, local Python test-runs with JSON-object arguments and configured `valves` object injection behind `tools.execute`, persisted local tool run history, audit events, and workspace-backup inclusion; direct tool-server registry implemented for stdio/HTTP configs with `tools.write` enforcement for registry write operations and run-history deletion, `tools.execute` enforcement for direct HTTP invocation, direct stdio invocation, and MCP tool calls, persistence/import/export, lightweight status checks, HTTP and stdio MCP initialize/tools-list discovery, basic HTTP and stdio MCP `tools/call`, schema-seeded JSON argument editing, editable direct invocation JSON payloads, required/type/nullable-union/const/anyOf/oneOf/allOf/nested/enumerated/string/numeric/pattern/multiple/unique/additional-property argument validation, HTTP JSON invocation, stdio stdin/stdout/stderr/exit-code capture, persisted run history, readable audit events, and content-free run deletion markers; nested/generated advanced schema forms, richer sandboxing, long-running stdio lifecycle controls, and provider tool-call routing planned | 5/6 |
| Skills | `routers/skills.py`, `models/skills.py` | Native skill library implemented with create/edit/delete, active state, tags, user/group access grants, text/tag/active-state search, grant-aware active-skill chat system-context injection, disabled-feature blocking for skill writes/imports/file and open-panel import entry points/sharing/chat-context injection, `skills.write` admin-directory enforcement for write operations, JSON persistence, local audit events for create/update/delete, JSON export/import, raw Open WebUI skill-record export, single-skill native sharing, and tolerant Open WebUI-style skill record import with `access_grants` preservation; server-backed access enforcement and richer access-picker UX planned | 5.5 |
| Functions | `routers/functions.py`, `models/functions.py` | Native function library implemented with create/edit/delete, filter/action/pipe types, active/global flags, JSON-object valves editing and validation, schema-derived valves JSON defaults, generated primitive valves field controls, save-time valves schema validation, `functions.write` admin-directory enforcement for write operations, disabled-feature blocking for function create/update/delete/import/share plus local Python test-runs, `Valves` schema discovery, action affordances, and pipe model surfacing, `functions.execute` enforcement for local Python test-runs and chat actions, active filters, active single-pipe and manifold-pipe chat models, active `filter` `inlet`/`outlet` routing around provider chat streams, active single `pipe` functions and `pipes()` manifold submodels surfaced as local selectable chat models, local pipe chat execution through parent functions for selected manifold submodels, active `action` functions surfaced from assistant-message menus with selected-message/thread/history context, JSON persistence, local audit events for create/update/delete/invoke, native/Open WebUI JSON export/import, single-function native sharing, workspace-backup run history, recent run previews, and tolerant Open WebUI-style function record import; nested/generated advanced visual valves forms, stronger sandboxing, and richer execution audit detail planned | 6.5 |
| Users/roles/groups | `routers/users.py`, `routers/groups.py`, permission constants | Local native admin directory foundation implemented with users, admin/user/pending roles, groups, member assignment, flat permission grants, JSON persistence, `settings.write` enforcement for user/group create/update/delete, membership changes, and user-facing admin directory import/export, local audit events for user/group create/update/delete, membership changes, and admin directory import/export without directory content, sidebar management UI, native JSON export/import, tolerant Open WebUI-shaped user/group import with nested permissions flattened into native keys, SCIM-shaped ListResponse user/group import, SCIM user `groups` membership import, SCIM role/userType mapping into native admin/user/pending roles, and SCIM group entitlement import into native flat permissions; server-backed enforcement, invitations, profile metadata, LDAP/SSO/SCIM provisioning, and enterprise policy workflows planned | 5 |
| Analytics | `routers/analytics.py` gated by config | Native local analytics dashboard implemented with chat/message/model usage summaries, user/group filters for chat and model analytics, selectable model chat drilldowns, daily model activity buckets, persisted assistant generation duration metadata, provider token usage totals, feedback sentiment counts, knowledge/channel/note/automation/calendar workspace counts, web-search network-transparency summary tiles from local audit events, recent audit events, analytics JSON export with the same secretless web-search network summary and aggregate-only audit metadata, audit JSON export, local-only privacy copy, sidebar routing, and default feature-toggle enablement; richer charts and server-backed admin analytics remain planned | 5 |
| Evaluations/feedback | `routers/evaluations.py`, `models/feedbacks.py` | Native feedback records implemented from assistant messages with rating, reason/comment, chat/message metadata, snapshot, local moderation status controls, delete confirmations, local audit events, JSON persistence, native/Open WebUI JSON export, JSON import with count-only audit metadata, tolerant Open WebUI-style feedback record import, Elo-style model summaries, top tags, native evaluations dashboard, and query-weighted searchable feedback admin list; bulk/permissioned admin workflows planned | 5 |
| Settings/connections | admin settings routes, `AddConnectionModal.svelte`, config feature flags | Native provider settings support Ollama base URL with malformed-URL preflight before settings writes, guarded active-provider switching that rejects unknown provider IDs before persistence and clears stale selection errors after valid switches, explicit provider health checks, active-provider native capability summaries, OpenAI-compatible add/update/remove with malformed-base-URL preflight before settings or Keychain writes, Keychain-backed API keys, embedding model selection, persisted feature toggles, Settings UI toggles for implemented native surfaces, workspace backup export/import, and sidebar gating; broader admin settings and unsupported/planned feature workflows remain planned | 1/5 |
| Database/export tools | admin export/database tools and per-surface import/export routes | Native full-workspace JSON backup/restore implemented for settings, chats, folders, local files, knowledge, workspace libraries, local tool run history, local function run history, direct tool-server registry and invocation history, terminal sessions and command transcripts, admin directory, channels, automations, automation run history, calendar, notes, feedback, playground history, audio history, and audit events; Keychain secret values are intentionally excluded and must be re-entered after cross-machine restore | 5/7 |
| Notes | `routes/(app)/notes`, `routers/notes.py` | Native local notes implemented with create/edit/delete, pin/unpin, pinned-first sorting, title/content search, durable app-local note links with copy-link and link-resolution focus behavior, native Markdown sharing, disabled-feature blocking for note create/update/pin/delete, JSON import persistence, note-to-chat attachment, native Markdown sharing, and note-to-knowledge import/indexing, `notes.write` admin-directory enforcement for write operations, non-content audit events for note create/update/pin/delete, JSON persistence, native JSON export/import, raw Open WebUI note-record export, tolerant Open WebUI-style JSON import, note-to-chat context attachment, and note-to-knowledge collection import/indexing; collaboration metadata planned | 6 |
| Channels | `routes/(app)/channels`, `routers/channels.py`, socket channel events | Native local channel MVP implemented with create/edit/delete plus lifecycle audit events, channel search, message posting, thread replies, unread counts, local members with role/status/mute/pin state and non-content member management audit events, disabled-feature blocking for channel create/update/delete/message/reply/member/import/select mutations, `channels.write` admin-directory enforcement for local write operations, JSON persistence, JSON export/import, raw Open WebUI channel-record export, tolerant Open WebUI-shaped channel/member/reply record import, sidebar routing, and a native channel detail/composer/member/reply surface; realtime socket events, server-backed membership enforcement, channel tools/search integration, and full Open WebUI migration remain planned | 6 |
| Automations | `routes/(app)/automations`, `routers/automations.py`, `models/automations.py` | Native local automation library foundation implemented with create/edit/delete, enable/pause, non-prompt lifecycle audit events, run-now execution through the active provider, active-provider chat capability gating with failed-run persistence, disabled-feature blocking before create/update/toggle/delete, manual run-now, due-run execution, provider streaming, JSON import persistence changes, open-panel import, and single-automation sharing, `automations.write` admin-directory enforcement for local write and run operations, persisted success/failure run history and audit events, daily/weekly next-run calculation, live RRULE validation and next-run preview in the editor, store-level rejection for unsupported schedules, feature-toggle-gated app-open due automation scheduler with immediate stop on disable, virtual Scheduled Tasks calendar projection, next-run/scheduler UI status, search including `model:` and `status:` operators, JSON persistence, native JSON export/import, raw Open WebUI automation-record export, single-automation native sharing, tolerant Open WebUI-shaped automation record import with `data.prompt`, `data.model_id`, `data.rrule`, metadata and run timestamps preserved; OS-level closed-app scheduler, limits, terminal config execution, and notifications remain planned | 6 |
| Calendar | `routes/(app)/calendar`, `routers/calendar.py`, `models/calendar.py` | Native local calendar foundation implemented with default Personal calendar creation, calendar creation/deletion with user/group access grants, grant-aware visible calendars and event search/range/reminder sources, event create/edit/delete/cancel, attendee add/update/remove with RSVP status persistence, disabled-feature blocking before calendar/event/attendee mutations, JSON import persistence, reminder delivery, reminder scheduler starts, and single-event sharing, `calendar.write` admin-directory enforcement for local calendar/event/attendee write operations, optional local reminder offsets, due-reminder query support, app-open reminder notification scheduling through `UNUserNotificationCenter`, optional RRULE entry in the event editor, daily/weekly/monthly recurring occurrence expansion with `INTERVAL`, `COUNT`, `UNTIL`, weekly `BYDAY`, and monthly `BYMONTHDAY`, safe original-event fallback for unsupported recurrence rules, virtual read-only Scheduled Tasks calendar from active automations, native event search with free text plus `calendar:` and `status:` operators, month grid with leading/trailing days and event bucketing, week grid respecting the user's first weekday, day schedule with 24 hour slots, agenda rows, recurring date-range filtering, attendee metadata preservation, JSON persistence, native JSON export/import, raw Open WebUI calendar/event export with access grants and attendees, single-event native sharing, tolerant Open WebUI-shaped calendar/event/attendee import with reminder metadata and nanosecond timestamp conversion, sidebar routing, and native dashboard/editor surface; advanced RRULE support beyond `COUNT`/`UNTIL`, richer shared calendars, closed-app reminder guarantees, and server-backed permissions remain planned | 6.5 |
| Playground | `routes/(app)/playground/*`, `components/playground/Chat.svelte` | Native Chat, Completions, Notes, and Images playground foundation implemented with sidebar routing, mode switching, temporary system/user prompt editors for chat, raw prompt completion mode, note selection/title/body drafting with save/update into the native notes library, image prompt/model/size/quality/count controls, model selection, optional side-by-side chat model comparison, temperature/top-p/max-token parameter controls, active-provider chat streaming, active-provider completion streaming, active-provider image generation, provider capability gating for chat/completions/images modes, disabled-feature blocking before provider calls, `playground.execute` admin-directory enforcement before provider calls, `notes.write` enforcement before Playground note saves, `playground.write` enforcement for saved-run history mutation, local preset/history save-load-delete, text and image output/error state, text export, JSON export preserving mode/comparison/options/image outputs, current-run and saved-run native JSON sharing, and default feature-toggle enablement; richer provider capability gating remains planned | 6 |
| Image generation/editing | `routers/images.py`, image config flags | Native image generation/editing foundation implemented with sidebar/detail surface, model/prompt/size/quality/count controls, OpenAI-compatible `/images/generations`, `/images/edits`, and DALL-E 2 `/images/variations` provider paths, multipart single-image edit uploads with optional mask image uploads, multipart variation uploads with base64 output, base64 image decoding, disabled-feature blocking before generate/edit/variation provider calls and generated-image data/file/open-panel import/export entry points, `image_generation.execute` admin-directory enforcement before provider calls, `image_generation.write` enforcement before generated-image library import/export user actions, generated image metadata records with source-image linkage for edits and variations, gallery rendering, local JSON persistence, native image-library JSON export/import with count-only audit metadata, raw Open WebUI/OpenAI-style image response export/import with `b64_json` records, workspace-backup inclusion, default feature-toggle enablement, provider capability filtering, and provider error display; multi-image reference editing and broader media permissions remain planned | 6 |
| Audio STT/TTS and voice mode | `routers/audio.py`, audio config flags, voice-mode UI | Native STT/TTS foundation implemented with sidebar/detail surface, imported audio selection, permission-aware live microphone record/stop capture into pending `.m4a` audio, app-bundle microphone usage metadata, OpenAI-compatible `/audio/transcriptions` multipart requests, OpenAI-compatible `/audio/speech` JSON requests, native Voice Chat button for pending-audio voice-mode orchestration from transcription to normal chat streaming to optional synthesized assistant reply, separate default-enabled Voice Mode feature toggle, disabled-feature blocking before STT/TTS/voice provider calls, microphone recording, pending-audio file/open-panel import, audio-history data/file/open-panel import entry points, audio-history user-action export, generated-speech playback, and saved audio-history load/play/delete actions, `audio.transcribe` admin-directory enforcement before STT provider calls and microphone recording, `audio.synthesize` enforcement before TTS provider calls, `audio.write` enforcement before audio-history deletion/import/export user actions, transcript editing, provider model-name filtering/defaults for likely STT/TTS models, synthesized audio export, local playback controls for generated speech and speech history, persisted transcription/speech history, history reload/delete/play, native audio-history JSON export/import with count-only audit metadata, workspace-backup inclusion, default feature-toggle enablement, provider capability filtering, and provider error display; local audio adapters, formal per-model media capability metadata, and realtime duplex voice conversation remain planned | 7 |
| Web search | config `ENABLE_WEB_SEARCH`, retrieval web loaders | Native web-search foundation implemented with composer opt-in, default feature-toggle enablement, normalized DuckDuckGo HTML/SearXNG/Brave Search admin settings, Keychain-backed Brave API-key storage, official Brave endpoint/header request construction, clamped result/page-text limits, deduplicated domain filters, optional page-content loading with HTML/plain-text cleanup, selectable-PDF text extraction, disabled-feature blocking before search/provider calls, `web_search.execute` admin-directory enforcement before search network calls, search request parsing, web citations, provider-context insertion, compact composer result previews with freshness metadata and secretless contacted-host/API-key-use network summaries, local success/failure telemetry, secretless local audit-history events for completed/failed/blocked web searches, Analytics network summary tiles and analytics JSON export coverage for runs/hosts/API-key use/blocked-failed outcomes plus top contacted hosts, page-text enrichment counts, stale-preview clearing, and pre-provider error display; broader binary web extraction, additional API-key engines, and richer network transparency history drilldowns remain planned | 6 |
| Code interpreter | config `ENABLE_CODE_INTERPRETER`, Pyodide workers | Native opt-in shell/Python runner implemented with explicit user-triggered execution, disabled-feature blocking before local process launch and run-history deletion, `code.execute` admin-directory enforcement for running code and deleting run history, stdout/stderr/exit-code capture, timeout termination, configurable language/directory/timeout policy, executable allow/deny preflight policy, blocked-run errors before launch, local JSON run history with content-free deletion audit markers, workspace-backup inclusion, and sidebar/detail UI; stronger sandboxing, provider tool-call routing, and browser/Pyodide parity remain planned | 6 |
| Terminals/tool servers | `routers/terminals.py`, MCP/tool server handling | Direct tool-server registry foundation implemented behind an opt-in feature toggle with stdio/HTTP records, permission-aware sidebar management, disabled-feature blocking before status checks, discovery, direct invocation, and MCP tool calls, `tools.write` enforcement for registry write operations and run-history deletion, `tools.execute` enforcement for direct HTTP invocation, direct stdio invocation, and MCP tool calls, import/export, workspace-backup inclusion, lightweight command/HTTP connection checks, HTTP and stdio MCP initialize/tools-list discovery, basic HTTP and stdio MCP `tools/call`, schema-seeded JSON argument editing, editable direct invocation JSON payloads, required/type/nullable-union/const/anyOf/oneOf/allOf/nested/enumerated/string/numeric/pattern/multiple/unique/additional-property argument validation, HTTP JSON invocation, stdio stdin/stdout/stderr/exit-code capture, persisted run history, readable audit events, and content-free run deletion markers; native terminal session foundation implemented behind an opt-in feature toggle with `terminal.execute` shell-run enforcement, `terminal.write` session/transcript management enforcement, execute-only auto-session creation, editable session title/working directory, shell policy reuse, one-command-at-a-time persisted transcripts, transcript rerun drafting, session/transcript deletion with content-free audit markers, sidebar/detail UI, readable audit events, and workspace-backup inclusion; live PTY streaming, long-running stdio lifecycle controls, generated advanced schema forms, SSE/session hardening, and provider tool-call routing planned | 6 |
| OAuth/LDAP/SCIM | OAuth routes, LDAP deps, optional `routers/scim.py` | SCIM-shaped user/group, user-membership, role, and group-entitlement import into the local admin directory is implemented; live OAuth/LDAP/SCIM provisioning remains planned where feasible | 7 |
| Audit/privacy | audit config in `main.py`, telemetry utils | Local audit-log foundation implemented for feature-toggle changes, provider settings changes, analytics export with aggregate-only metadata and secretless web-search network-summary counts, feedback import/export with count-only metadata, generated-image import/export with count-only metadata, audio-history import/export with count-only metadata, prompt create/update/delete and import/export with count-only metadata, tool create/update/delete, note create/update/pin/delete without note content, skill create/update/delete, function create/update/delete, admin user/group create/update/delete, group membership changes, admin directory import/export without directory content, workspace backup import/export with count-only metadata and no workspace content, audit-log export with count-only metadata, channel create/update/delete and member management, calendar event/attendee changes without event content, feedback moderation/deletion, automation lifecycle changes without prompt content, automation runs, web-search runs with secretless contacted-host/API-key-use metadata and no query content plus local Analytics summary/export coverage, code-execution runs, terminal session creation/update/deletion and command run/deletion without deleted transcript content, and direct tool-server invocation/run-deletion events without deleted request/response payloads, with JSON persistence, button/Escape-clearable searchable Analytics display including match counts and readable expandable event metadata, export/delete controls that leave deletion markers, workspace-backup inclusion, and no telemetry; broader enterprise audit coverage and privacy/network audit remain planned | 7 |

Legend:
- MVP in progress: included in the first native macOS implementation slice.
- Planned: known requirement with no native implementation yet.
- Blocked: only implemented if a supported external API exists.
