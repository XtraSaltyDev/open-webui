# Native macOS Testing

## Current Commands

```bash
swift test
swift build
./script/build_and_run.sh --package
./script/build_and_run.sh --smoke
./script/build_and_run.sh --validate-package
./script/build_and_run.sh --verify
git diff --check
```

Use `docs/native-macos/manual-acceptance.md` as the post-automation daily-driver click-through checklist.

## Current Coverage

- Ollama runtime health-check request flow, including version, installed model count, running model count, and model listing request/response decoding.
- Ollama model pull request body and streaming progress decoding.
- Ollama model delete request body.
- Ollama streaming chat request body, generation option mapping, JSON-line delta parsing, and worker cancellation when consumers stop.
- `AppStore` Ollama pull/delete state, model-management capability guards before manager calls, model refresh, and selected-model cleanup.
- OpenAI-compatible model listing with bearer API-key auth.
- OpenAI-compatible streaming chat-completion and raw completion request bodies, generation option mapping, SSE delta parsing, and worker cancellation when consumers stop.
- OpenAI-compatible embeddings request body and vector decoding.
- OpenAI-compatible image generation request body, multipart image edit request body, multipart image variation request body, bearer auth, base64 image decoding, image metadata decoding, and unsupported active-provider image guard behavior.
- OpenAI-compatible audio transcription multipart request body, speech synthesis JSON request body, bearer auth, transcript decoding, synthesized audio byte handling, and unsupported active-provider audio guard behavior.
- Explicit active-provider health-check status routing, Settings-facing provider health status copy/icon/help presentation, unknown-provider selection rejection before settings persistence, Settings-facing malformed Ollama base-URL save-button preflight and inline recovery messaging, malformed Ollama base-URL rejection before settings persistence, Ollama provider settings audit logging, Settings-facing provider picker option filtering/active-state/detail presentation, and Settings-facing provider capability summary formatting.
- OpenAI-compatible provider setup with Settings-facing form defaults, secretless metadata presentation, state-aware Keychain API-key help copy, malformed-base-URL save-button preflight and inline recovery messaging, malformed-base-URL rejection before settings or Keychain secret persistence, non-secret provider settings audit logging, plus provider removal with Keychain secret cleanup, non-secret removal audit logging, Ollama fallback, and selected-model reset.
- OpenAI account-access policy blocking unsupported ChatGPT subscription model access, cookie reuse, and private endpoint access, with official reference evidence and last-review date coverage.
- JSON chat persistence round trip, persisted chat rename, chat cloning with fresh message IDs, pin/unpin persistence, pinned-first sorting, archive/unarchive filtering and persistence, archive-all/unarchive-all persistence, delete-all persistence, all-chat and archived-chat JSON export, all-chat export-bundle import with fresh local IDs, selected-chat fallback after archiving, and legacy chat decoding.
- Chat message edit, rating, regenerate behavior, unsupported active-provider regenerate guard behavior before clearing old content, stale streaming-message normalization on load, composer empty-send inline messaging, draft/attachment preservation when a send is blocked before provider streaming, successful-send draft clearing, and Markdown share handoff.
- Multi-model settings migration, multi-select persistence, one-prompt-to-many-assistant fan-out, assistant generation duration recording/formatting, provider token usage recording, per-branch failure handling, concurrent branch streaming, active-branch progress text, per-branch cancellation, underlying provider-stream cancellation, active-send cancellation, original-thread finalization after selecting another chat during streaming, no duplicate assistant messages after cancellation, and unsupported active-provider chat guard behavior before thread creation.
- Markdown message segmentation, native attributed markdown conversion, malformed-markdown fallback, fenced code block extraction, lightweight code-token classification, unsupported-language plain fallback, and inline/block LaTeX math segmentation outside code fences.
- Markdown chat export, single-chat JSON export round trip including assistant generation metrics and provider token usage, Open WebUI-compatible selected-chat and all-chat export envelope generation, native JSON chat import persistence, Open WebUI chat record active-branch import, and Open WebUI `{ "chats": [...] }` envelope import.
- Folder persistence, chat assignment, folder deletion without deleting chats, and disabled-feature blocking for folder create/assignment/delete actions.
- Prompt library create/update/delete persistence with audit events, command persistence/normalization, tag/category persistence and normalization, user/group access-grant native JSON and Open WebUI `access_grants` round trips, prompt version-history snapshots on update, native JSON version round trips, slash-command composer expansion, variable-backed command safety errors, composer insertion, `{{variable}}` extraction and resolved insertion, missing variable errors, disabled-feature blocking for prompt create/update/delete/import/file and open-panel import entry points/insert/command lookup/share, `prompts.write` admin-directory enforcement for write operations, native JSON export/import round trip, raw Open WebUI prompt-record export, count-only prompt import/export audit events without prompt content, single-prompt JSON export/share, and Open WebUI-style prompt JSON import.
- Tool library create/update/delete persistence with audit events that omit source content and valves, JSON-object valves editing/persistence/reload and invalid-valves rejection, schema-derived valves JSON default generation, schema field metadata extraction including nullable/union types, primitive schema field default/type fallback reads, top-level field JSON mutation, invalid field-editor body rejection, save-time valves schema validation, `tools.write` admin-directory enforcement for write operations, disabled-feature blocking for tool create/update/delete/import/share plus local tool runs and `Valves` schema discovery, master Local Execution blocking before local tool runs and valves-schema subprocesses, JSON export/import round trip, raw Open WebUI tool-record export, single-tool JSON export/share, Open WebUI-style tool JSON import with nested specs and valves preserved, `tools.execute` enforcement before local tool runs, local Python `Tools` method invocation with JSON-object arguments, configured valves object injection, local Python `Valves` schema discovery, failure capture, persisted run history, local tool run audit events, and workspace-backup inclusion.
- Direct tool-server registry create/update/delete persistence, `tools.write` enforcement for registry write operations and run-history deletion, `tools.execute` enforcement before direct HTTP invocation, direct stdio invocation, and MCP tool calls, master Local Execution blocking for stdio discovery/invocation/tool calls while HTTP tool-server calls remain available, stdio argument/environment normalization, stdio subprocess MCP initialize/tools-list discovery, stdio subprocess stderr draining and close cleanup, stdio subprocess MCP `tools/call`, stdio stdin/stdout/stderr/exit-code invocation capture, HTTP URL records, command/HTTP status checks, transient AppStore status updates, HTTP MCP initialize/tools-list discovery, JSON-RPC error surfacing, discovered-tool AppStore state, disabled-server discovery blocking, basic HTTP MCP `tools/call`, schema-seeded JSON argument templates, editable direct invocation JSON payload routing, required/type/nullable-union/const/anyOf/oneOf/allOf/nested/enumerated/string/numeric/pattern/multiple/unique/additional-property argument validation, invalid tool-call argument blocking, persisted tool-call run history, HTTP JSON invocation request/response capture, disabled-server invocation blocking, persisted invocation run history, readable invocation/deletion audit events with content-free deletion markers, JSON export/import round trip, Open WebUI-shaped import, feature-toggle sidebar compile coverage, and workspace-backup inclusion.
- Automation daily/weekly schedule calculation, RRULE validation/next-run preview, active due-run filtering, AppStore due automation execution, app-open scheduler start/stop execution, feature-toggle disabled scheduler blocking, disabled-feature blocking before manual automation actions, provider streaming, import persistence, and single-automation sharing, immediate scheduler stop when Automations is disabled, and post-run next-run rescheduling.
- Skill library create/update/delete persistence with audit events that omit skill content, active state, tag/access-grant normalization, text/tag/active-state search, grant-aware active-skill chat system-context injection for direct user grants and group membership grants, disabled-feature blocking for skill create/update/delete/import/file and open-panel import entry points/share/chat-context injection, `skills.write` admin-directory enforcement for write operations, JSON export/import round trip, single-skill JSON export/share, raw Open WebUI skill-record export, and Open WebUI-style skill JSON import with `access_grants`.
- Function library create/update/delete persistence with audit events that omit source content, manifests, and valves, filter/action/pipe type handling, active/global flag preservation, JSON-object valves editing/persistence/reload and invalid-valves rejection, schema-derived valves JSON default generation, schema field metadata extraction, primitive schema field default/type fallback reads, top-level field JSON mutation, invalid field-editor body rejection, save-time valves schema validation, `functions.write` admin-directory enforcement for write operations, disabled-feature blocking for function create/update/delete/import/share plus local function runs, `Valves` schema discovery, action affordances, and pipe model surfacing, master Local Execution blocking before local function runs, valves-schema subprocesses, filters, pipes, manifold discovery, and assistant actions, `functions.execute` enforcement before local function runs and assistant-message actions, local Python function-method invocation with JSON-object input, local Python `Valves` schema discovery, active `filter` `inlet` transformation before provider chat streaming, active `filter` `outlet` transformation after provider chat streaming, active single `pipe` function surfacing as selectable local chat models, active manifold `pipes()` submodel discovery without persisted run-history noise, local single and manifold pipe chat execution without invoking the network provider, local pipe selection not enabling Ollama model deletion, active `action` function discovery and selected-message/thread/history context routing, failure capture, persisted run history, local function run audit events, JSON export/import round trip, raw Open WebUI function-record export, single-function JSON export/share, workspace-backup inclusion, and Open WebUI-style function JSON import with manifest and valves preserved.
- Feedback record creation from assistant messages, message-rating sync, reason/comment trimming, moderation status persistence/update with audit event, feedback deletion persistence with audit event, JSON persistence/reload, JSON export/import round trip, raw Open WebUI feedback-record export, count-only feedback import/export audit events without comments/reasons/chat snapshots, and Open WebUI-style feedback JSON import.
- Feedback evaluation summary calculation for multi-model Elo-style wins/losses, single-model feedback volume/sentiment, top tags, missing-model filtering, query-weighted admin list filtering/search including moderation status, and AppStore dashboard selection state.
- Local analytics summary calculation for chat/message/model usage, user/group chat and model analytics filters, daily model activity bucketing, feedback sentiment, workspace counts, model usage sorting, JSON export including secretless web-search network summary data, older analytics-export bundle decoding without that newer field, and AppStore analytics dashboard selection state.
- Local audit event JSON persistence newest-first, feature-toggle audit logging/reload, provider settings audit logging without secret values, analytics export audit logging without report content and with secretless web-search run/host/API-key-use/failed/blocked counts, feedback import/export audit logging without feedback content, generated-image import/export audit logging without image bytes or prompts, prompt import/export audit logging without prompt content, prompt/tool/skill/function library audit logging/reload, note create/update/pin/delete audit logging without note content, local tool/function invocation audit logging, direct tool-server run deletion audit logging without request/response payloads, web-search network audit history without queries or secret values, web-search network history summary counts for outcomes/hosts/API-key use, terminal session/command audit logging without deleted transcript content, admin directory audit logging/reload, non-content admin directory import/export audit events, count-only workspace backup import/export audit events, count-only audit-log export events, feedback admin audit logging/reload, channel lifecycle/member audit logging, calendar event/attendee audit logging without event content, code-execution audit logging, readable audit metadata formatting with analytics-export web-search count rows, feedback-transfer/generated-image-transfer/prompt-transfer/prompt/tool/skill/function/admin/admin-directory/workspace-backup/audit-log-export/web-search/code-execution/terminal/direct-tool-server-specific rows, camelCase/acronym fallback labels, and preview/overflow rows, audit event search across action ids, action/outcome labels, summary, and metadata, active-query detection, clear-query behavior, clear-search tooltip and empty-result hints, audit search result summaries, audit JSON export including the export marker, audit deletion with a persisted deletion marker, and workspace-backup inclusion.
- Admin directory user create/update/delete persistence with audit events, role changes, group creation/update/delete with audit events, member assignment audit events, `settings.write` enforcement for user/group mutations, member changes, and user-facing import/export, non-content import/export audit events, flat permission checks, admin allow-all behavior, pending-user deny behavior, JSON export/import round trip, Open WebUI-shaped user/group import with nested permission flattening, SCIM-shaped ListResponse user/group import with membership cleanup, SCIM user `groups` membership import, SCIM role/userType mapping, and SCIM group entitlement permission import.
- Feature toggle defaults for legacy settings, stale/empty provider migration, guarded provider selection with stale-error recovery, settings JSON round trip, AppStore toggle persistence/reload, and SwiftUI compile coverage for gated sidebar/settings surfaces.
- Attachment import persistence for reusable file-library records, original imported byte preservation and original-file export, Files-only binary import without draft mutation, Files free-text and wildcard filename search filtering, saved-file extracted-text sharing, binary-only original-file sharing, saved-file extracted-text copy/export/editing with pending-attachment sync for edits, binary-only attach blocking until extracted text exists, native/Open WebUI-shaped Files JSON transfer with native original-byte preservation, disabled-feature blocking for saved file-library import/use/mutation/JSON import/file and open-panel JSON import entry points, Files rename persistence and pending-attachment sync, Files surface routing, Files feature-toggle close behavior, saved-file deletion and delete-all cleanup with matching pending-attachment cleanup, attach-from-saved-file composer behavior, provider-context reuse through normal `ChatAttachment` records, message/export attachment persistence, and workspace-backup inclusion.
- Notes create/update/delete persistence with non-content audit events, pin/unpin persistence with non-content audit events, pinned-first sorting, legacy note decoding, title/content search filtering, durable app-local link generation/resolution and linked-note focus behavior, native Markdown sharing, disabled-feature blocking for note create/update/pin/delete, JSON import persistence, note-to-chat attachment, native Markdown sharing, and note-to-knowledge import/indexing, `notes.write` admin-directory enforcement for write operations, native JSON export/import round trip, raw Open WebUI note-record export, Open WebUI-style note JSON import, note-to-chat context attachment, and note-to-knowledge import/indexing.
- Channel create/update/delete persistence with lifecycle audit events, unread-clearing, search, thread reply persistence, reply import/export, member add/update/remove persistence with non-content audit events, member import/export, disabled-feature blocking for channel create/update/delete/message/reply/member/import/select mutations, `channels.write` admin-directory enforcement for local write operations, JSON export/import round trip, raw Open WebUI channel-record export, Open WebUI-shaped import, and sidebar/detail compile coverage.
- Automation create/update/delete persistence with non-prompt lifecycle audit events, create/update rejection for unsupported schedules, enable/pause persistence with non-prompt audit events, run-now success/failure persistence, provider prompt routing, unsupported active-provider chat guard behavior before streaming, disabled-feature blocking before create/update/toggle/delete, run-now, due-runs, provider calls, and import persistence, last-run timestamp updates, run-history reload, success/failure audit event metadata, `automations.write` admin-directory enforcement for local write and run operations, JSON export/import round trip, single-automation JSON export/share, raw Open WebUI automation-record export, Open WebUI-style automation JSON import with nested data/meta/run timestamps preserved, and automation search with `model:`/`status:` operators.
- Calendar default creation, virtual Scheduled Tasks calendar visibility, user/group calendar access grants, grant-aware visible calendar lists, grant-aware event range/search filtering, calendar grant JSON export/import round trip, active automation occurrence projection, inactive automation exclusion, event search across text, calendar, cancelled status, and virtual Scheduled Tasks events, calendar event create/update/cancel/delete persistence with non-content audit events, attendee add/update/remove persistence with non-content audit events, duplicate attendee update behavior, event reminder create/update/reload persistence, local due-reminder range querying, app-open reminder notification request delivery and duplicate suppression, reminder scheduler start/stop behavior, disabled-feature blocking before calendar/event/attendee mutations, JSON import persistence, reminder delivery, reminder scheduler starts, and single-event sharing, event RRULE persistence, `calendar.write` admin-directory enforcement for local calendar/event/attendee write operations, daily/weekly/monthly/simple-yearly recurring occurrence expansion with `COUNT` and `UNTIL` limits, yearly `BYMONTH` ordering/count behavior, unsupported recurrence fallback to the original event, month-grid date math, leading/trailing day cells, recurring month event bucketing, week-grid date math, first-weekday handling, day-schedule 24-hour slot generation, all-day separation, single-day and multi-day event bucketing, overnight event bucketing, recurring date-range event filtering, JSON export/import round trip, raw Open WebUI calendar/event export with access grants and attendees, single-event JSON export/share, Open WebUI-style calendar/event/attendee import with reminder metadata and nanosecond timestamp conversion, and native calendar sidebar/detail compile coverage.
- Playground provider streaming, system/user message construction, selected/default model routing, side-by-side comparison routing, raw completion request routing without chat messages, Playground note create/update routing into native note storage without provider calls, temperature/top-p/max-token option forwarding, image playground request routing, temporary image output state, image saved-run persistence/reload, unsupported active-provider chat guard behavior before streaming, disabled-feature blocking before provider calls, `playground.execute` admin-directory enforcement before provider calls, `notes.write` enforcement before Playground note saves, `playground.write` enforcement for saved-run history mutation, saved-run history persistence/reload, provider error display, no-chat-thread persistence, text export, JSON export with mode/comparison/options/image-output preservation, current and saved run JSON sharing, current transcript construction, and AppStore playground dashboard selection state.
- Image generation dashboard selection, active-provider generation/edit/variation request routing, optional edit-mask state and multipart upload routing, disabled-feature blocking before generate/edit/variation provider calls and generated-image data/file/open-panel import/export entry points, `image_generation.execute` admin-directory enforcement before provider calls, `image_generation.write` enforcement before generated-image library import/export user actions, generated image metadata state, edited-image and variation source linkage, local JSON persistence/reload, native image-library JSON export/import, raw Open WebUI/OpenAI-style image response export/import with `b64_json` records, count-only image-library import/export audit events without image bytes or prompts, workspace-backup inclusion, feature-toggle default enablement, and provider error display.
- App bundle metadata template coverage for required bundle identity fields, `NSMicrophoneUsageDescription`, and the `openwebui-native` URL scheme; `./script/build_and_run.sh --package` creates `dist/OpenWebUINative.app` without launching it, and `./script/build_and_run.sh --validate-package` ad-hoc signs the bundle with hardened runtime options before strict local `codesign` verification.
- Audio dashboard selection, active-provider transcription/speech request routing, microphone permission request/denial handling, microphone recording start/stop handoff into pending audio, Voice Chat UI affordance enablement, voice-mode orchestration from pending audio to normal chat streaming to optional synthesized assistant reply, disabled-feature blocking before STT/TTS/voice provider calls, microphone recording, pending-audio file/open-panel import, audio-history data/file/open-panel import entry points, audio-history user-action export, generated-speech playback, and saved audio-history load/play/delete actions, separate Voice Mode feature-toggle default enablement, `audio.transcribe` admin-directory enforcement before STT provider calls and microphone recording, `audio.synthesize` enforcement before TTS provider calls, `audio.write` enforcement before audio-history deletion, import, and export user actions, pending imported or recorded audio state, transcript state, audio-specific provider model filtering/default selection, synthesized speech bytes/filename state, local playback play/pause/stop state, playback loading from speech history, persisted transcription/speech history, history reload/delete, native audio-history JSON export/import round trip with count-only audit metadata, feature-toggle default enablement, and provider error display.
- Web search settings persistence, decoded/admin-entered settings normalization, DuckDuckGo HTML request/parsing/domain filtering, SearXNG JSON request/result limiting, Brave Search endpoint/header/request parsing with Keychain-backed API-key lookup, Tavily endpoint/header/request parsing with Keychain-backed API-key lookup, missing Brave/Tavily API-key blocking before network requests, optional page-content loading with HTML cleanup, selectable-PDF text extraction, and length caps, composer opt-in chat routing, disabled-feature blocking before search/provider calls, `web_search.execute` admin-directory enforcement before search/provider calls, web citation creation, provider-context insertion with page-text preference and snippet fallback, recent result preview and freshness telemetry state, contacted-host and Keychain API-key-use network transparency summaries without secret values, persisted web-search audit events for completed and blocked searches without query/secret metadata, Analytics network transparency summary rendering and analytics JSON export coverage, no-network metadata for disabled/permission-blocked flows, stale-preview clearing, feature-toggle default enablement, and pre-provider error display.
- Secretless app diagnostics coverage for app paths, chat storage path, provider health, model selection, chat counts, selected chat ID/title, selected-message count, active streaming branch count, last notice, Local Execution state, and omission of Keychain secret IDs and message content.
- Local Execution defaults/migration/settings coverage, including disabled-by-default settings, accepted-risk requirement, sandbox-root normalization and creation, broad old-root migration to `~/OpenWebUINativeSandbox`, outside-sandbox blocking before subprocess launch, and test-only opt-in helpers for execution-focused AppStore tests.
- Bounded process runner coverage for concurrent stdout/stderr draining under large output, timeout termination, maximum captured-output truncation messaging, working-directory routing, and cancellation/cleanup behavior.
- Code-interpreter shell/Python execution capture, nonzero exit-code status, timeout termination, maximum captured-output truncation, policy decisions for master Local Execution, allowed language/directory/timeout/output caps, and executable allow/deny rules, standardized working-directory containment and sibling-prefix blocking, settings JSON persistence for execution policy, run-history JSON persistence/reload/deletion, content-free deletion audit markers without code/stdout/stderr, AppStore request routing, disabled-feature blocking before executor launch and run-history deletion, `code.execute` enforcement before run-history deletion, blocked-run errors before executor launch, empty-code errors, dashboard selection state, opt-in feature-toggle default, and workspace-backup inclusion.
- Direct tool-server disabled-feature blocking before status checks, discovery, direct invocation, and MCP tool calls; Local Execution blocking for stdio discovery/invocation/calls without blocking HTTP tool-server calls; bounded stdio MCP discovery/call timeouts, direct stdio invocation timeout/cancellation termination, normal stdio success behavior, direct tool-server run-history deletion with content-free audit markers; terminal session create/update/run/delete persistence, terminal command transcript deletion with content-free audit markers, transcript rerun draft preparation, feature-toggle blocking, Local Execution and `terminal.execute` shell-run enforcement, `terminal.write` session/transcript management enforcement, execute-only auto-session creation, shell policy reuse, audit events, storage round trip, and sidebar/detail selection; workspace backup JSON export/import round trip across major local surfaces, automatic safety backup before destructive restore/import with latest-ten retention, user-action backup import/export audit events without workspace content, restore replacement of stale local data, reload persistence after restore, code-execution history restore, terminal session/command restore, local tool/function run-history restore, audio-history restore, audit-event restore, direct tool-server registry/run-history restore, and explicit Keychain secret exclusion metadata.
- Tag normalization, tag persistence, tag removal, sidebar search filtering, `tag:`/`folder:`/`pinned:`/`archived:` chat search operators, native message transcript search result routing with focused-message selection state, and app-local chat/message link generation/routing.
- Text and selectable-PDF attachment import, pending attachment metadata, persisted message attachments, provider context formatting, and Markdown export attachment output.
- Knowledge text/PDF/native-note import, text chunking, document metadata population, legacy knowledge JSON metadata fallback, JSON knowledge persistence, JSON knowledge export/import round trip including collection access grants, single-collection JSON export/share containing only the selected collection's documents and chunks, embedding-backed retrieval, dedicated embedding model routing, provider-aware embedding model candidate filtering/defaulting, unknown-provider-model fallback to the active chat model, unsupported active-provider embedding guard behavior, disabled-feature blocking before knowledge create/update/import/reindex/share/delete/document-selection actions and `#collection` retrieval/provider chat, `knowledge.write` admin-directory enforcement for write operations, grant-aware collection visibility and `#collection` retrieval, direct-user and group-membership collection grants, `#collection` prompt context insertion, source-linked citation display data, citation-to-document opening with chunk focus, Markdown export citation output, file-based reindexing, collection rename/delete, document listing/detail/rename/delete, and indexed chunk source-name refresh after document rename.
- In-memory secret storage.
- Real macOS Keychain secret storage round trip.

## Next Test Targets

- Provider setup UI flows and end-to-end provider switching UI automation.
- Performance instrumentation.
- Full math typesetting snapshots and Shiki-style syntax-highlighted code block snapshots.
- OCR/scanned-PDF coverage, richer RAG document parser coverage, provider-reported embedding capability metadata, richer collection metadata editing beyond names, richer knowledge access-picker workflows, and production vector storage migration tests.
- UI tests for provider setup, model switching, chat persistence after restart, and error display.
- Manual acceptance runs for first-run setup, diagnostics, provider setup, Ollama/OpenAI-compatible chat, cancellation, retry/regenerate, exports, restart persistence, backup/restore, and Local Execution disabled-by-default checks.
- Richer analytics charting tests and bulk/permissioned feedback admin workflow tests.
- Realtime channel events, server-backed channel membership enforcement, channel search tools, admin controls, and full Open WebUI message/member migration tests.
- Automation closed-app scheduler due-claiming, terminal config, and notification/webhook tests.
- Broader RRULE support beyond the current daily/weekly/monthly/simple-yearly subset, closed-app reminder guarantees, richer shared calendar permissions, and saved-search/filter tests.
- Richer Playground provider capability gating tests and mode-specific export polish.
- Multi-image reference editing, deeper provider capability filtering, and media permission tests.
- Provider-reported audio per-model capability metadata, local audio adapter behavior, realtime duplex voice mode, Developer ID signing/notarization validation, and broader media permission tests.
- Broader binary web extraction, additional API-key search engines beyond Brave/Tavily, richer network transparency history drilldowns, and local status-history tests.
- OS-level local-execution sandboxing or entitlements, nested/generated advanced tool argument/valves forms, provider tool-call routing, live PTY terminal streaming, richer long-running stdio session controls, MCP SSE/session hardening, provider-driven tool-server invocation, and CPU/memory-style resource-limit tests.
